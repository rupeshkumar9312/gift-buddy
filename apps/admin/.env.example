# Copy to .env.local for local development.

NEXT_PUBLIC_API_URL=http://localhost:3001

# The storefront's public URL — used to build the tracking link included in
# the "Send WhatsApp Update" message on an order's detail page.
NEXT_PUBLIC_WEB_URL=http://localhost:3000
